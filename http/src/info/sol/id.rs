use serde::Serialize;

#[derive(Serialize)]
pub(crate) struct HSolInfoId {
    id: String,
}

////////////////////////////////////////////////////////////////////////////////////////////////////
// Conversions
////////////////////////////////////////////////////////////////////////////////////////////////////
impl HSolInfoId {
    pub(in crate::info::sol) fn from_id(sol_id: String) -> Self {
        Self { id: sol_id }
    }
}
