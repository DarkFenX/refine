// Specifies where effect should look for charge
#[derive(Copy, Clone)]
pub(crate) enum NEffectDmgKind {
    Turret,
    Missile,
    Breacher,
    Vorton,
    Bomb,
    Smartbomb,
    Superweapon,
}
