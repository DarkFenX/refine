pub(in crate::sol::svc::svce_calc) use fleet_upd::SolFleetUpdates;
use iter_loc_act::ActiveLocations;
pub(in crate::sol::svc::svce_calc) use main::SolModifierRegister;

mod debug;
mod fleet_upd;
mod iter_loc_act;
mod main;
